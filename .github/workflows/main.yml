name: Update Songs

on:
  push:
    branches:
      - master
    paths:
      - 'SONGS/**'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Update songs.json
        run: |
          echo "[" > songs.json
          first=true
          if [ -d "SONGS" ]; then
            for file in SONGS/*.mp4; do
              if [ -f "$file" ]; then
                if [ "$first" = "true" ]; then
                  first=false
                else
                  echo "," >> songs.json
                fi
                name=$(basename "$file" .mp4)
                fname=$(basename "$file")
                echo "  {\"name\": \"$name\", \"file\": \"$fname\"}" >> songs.json
              fi
            done
          fi
          echo "]" >> songs.json
      
      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # 1. FETCH + REBASE PRIMERO (importante)
          git fetch origin
          git rebase origin/master
          
          # 2. Agregar y commit
          git add songs.json
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "Auto update songs"
          
          # 3. PUSH
          git push
